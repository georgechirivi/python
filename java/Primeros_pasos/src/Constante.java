/* Que es una constante?
 * Espacio en la memoria del ordenador que es un valor  que no podra cambiar durante la ejecucion del programa
 * Por que hay que utilizar constantes
 * Por que a la hora de trabajar surge la necesidad de guardar datos temporalesmente que necesita ese mismo programa. Dichos datos deben ser fijos.
 * Como se cre una constatnte en java?
 * Utilizando la palabra reservada especificando el tipo de datos que almacenara en su interior + el nombre de la constante valor. 
 * P ej: final double a pulgadas = 2.54;
 * Recuerda:
 * El valor de una constante no podra cambiar.
 */
public class Constante {
    public static void main(String[] args) {
        int a = 5;
        int b;
        b= 7;
        int c=b+a;
        c++;N
        System.out.println(c);
    }
}
